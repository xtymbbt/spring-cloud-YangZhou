server:
  port: 83

spring:
  application:
    name: nacos-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: "http://localhost:8848/"

# 消费者将要访问的微服务名称（注册成功进nacos的服务提供者）
service-url:
  nacos-user-service: http://nacos-payment-provider